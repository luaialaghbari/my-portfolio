@media screen and (max-width: 1366px) {
/* Hero Modern Responsive Shared Layout (Tablet & Mobile) */
  .hero-modern {
    padding: 0 !important;
    min-height: 100svh;
    /* Use SVH for mobile browser bars */
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
  }

  .hero-modern-container {
    padding: 0;
    width: 100%;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    position: relative;
  }

  .hero-modern-grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    gap: 0;
    padding-top: 7vh;
    padding-bottom: 5vh;
    z-index: 10;
    padding-left: 0;
  }

  .hero-name-bg {
    position: relative;
    top: 0;
    left: 0;
    width: 100%;
    margin-top: 13vh;
    z-index: 5;
    display: flex;
    justify-content: center;
    pointer-events: none;
    overflow: visible;
  }

  .show-mobile {
    display: block !important;
  }

  .hide-mobile {
    display: none !important;
  }

  .hero-name-bg h1 {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    font-weight: 700;
    margin: 0;
    text-transform: none;
    line-height: 1.15;
    text-align: center;
  }

  .name-group {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: baseline;
    gap: 0.2em;
    width: 100%;
  }

  .hi-text {
    display: inline-block !important;
    font-size: 18vw;
    -webkit-text-stroke: 1px #fff;
    color: transparent;
    transform: scaleY(1.6);
    transform-origin: center top;
  }

  .hero-name-bg .mid-name {
    font-size: 18vw;
    /* Slightly smaller for shared base */
    background: var(--title-accent);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
    display: inline-block;
    transform: scaleY(1.6);
    transform-origin: center top;
    line-height: 1;
    z-index: 1;
    position: relative;
    margin-bottom: 0;
    margin-left: 0;
  }

  .hero-name-bg .last-name {
    font-size: 18vw;
    color: #fff;
    display: inline-block;
    transform: scaleY(1.6);
    transform-origin: center top;
    z-index: 2;
    position: relative;
    line-height: 1.1;
    margin-left: 0;
  }

  .hero-subtitle-container {
    margin-top: 0.6em;
    display: block;
    line-height: 1.2;
  }

  .dot-accent {
    color: #7c3aed;
  }

  .name-accent {
    display: block;
    margin-top: 0;
    font-size: 6.5vw;
    font-weight: 700;
    line-height: 1.1;
    background: var(--title-accent);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: transparent;
  }

  .hero-col-portrait {
    order: 1;
    height: auto;
    margin-top: -5vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    width: 100%;
    position: relative;
    z-index: 10;
    text-align: center;
  }

  .hero-portrait {
    position: relative;
    left: 0;
    width: 100%;
    height: auto;
    min-height: 50vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    margin: 0 auto;
  }

  .portrait-bg-shape {
    display: block !important;
    width: 75vw;
    max-width: 480px;
    height: 250px;
    left: 50%;
    bottom: 2%;
    transform: translateX(-50%) rotate(-6deg);
    border-radius: 55px 55px 0 0;
    -webkit-mask-image: none;
    mask-image: none;
    opacity: 0.95;
    z-index: 1;
    position: absolute;
    background: var(--title-accent);
    box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
  }

  .hero-portrait::after,
  .hero-modern.scrolled .hero-portrait::after {
    content: "";
    position: absolute;
    bottom: -10px;
    left: 0;
    width: 100%;
    height: 255px;
    background: linear-gradient(to top, #000000 45%, rgba(0, 0, 0, 0.9) 65%, transparent 100%);
    opacity: 1;
    z-index: 4;
    pointer-events: none;
  }

  .hero-portrait .hero-subtitle-container {
    margin-top: 3.5rem;
    display: block;
    text-align: center;
    width: 100%;
    pointer-events: auto;
    position: relative;
    z-index: 5;
  }

  .hero-portrait img {
    height: auto;
    width: 100%;
    max-width: 1100px;
    object-fit: contain;
    filter: drop-shadow(0 15px 40px rgba(0, 0, 0, 0.6));
    position: relative;
    z-index: 2;
    transform: scale(1.1);
    transform-origin: center center;
    margin: 0 auto;
    display: block;
  }

  .hero-col-content {
    order: 2;
    padding-top: 2vh;
    align-items: center;
    text-align: center;
    padding-bottom: 5vh;
    width: 100%;
  }

  .hero-text-block {
    width: 90%;
    max-width: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  .hero-description {
    text-align: center;
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.85);
    line-height: 1.6;
  }

  [dir="rtl"] .hero-col-content {
    align-items: center;
  }

  [dir="rtl"] .hero-description {
    text-align: center;
  }

  .hero-action-btn {
    margin-top: 4rem !important;
    z-index: 100;
    position: relative;
  }

  
}

